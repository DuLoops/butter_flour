import type { Metadata } from "next";
import { OrderProvider } from "@/lib/cartContext";
import "./globals.css";
import Nav from "@/components/ui/Nav";
import Footer from "./Footer";
import { Analytics } from '@vercel/analytics/next';

export const metadata: Metadata = {
  title: "ButterFlour",
  description: "Generated by create next app",
  icons: {
    icon: '/images/dev/logo3.png',
    shortcut: '/images/dev/logo3.png',
    apple: '/images/dev/logo3.png'
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link href="https://fonts.googleapis.com/css2?family=Meow+Script&family=Quicksand:wght@300..700&display=swap" rel="stylesheet" />
      </head>
        <OrderProvider>
      <body className={'font-qs bg-_white relative pb-20 min-h-screen'}>
          <main >
          <Nav />
            {children}
            <Analytics />
          <Footer />
          </main>
      </body> 
        </OrderProvider>
    </html>
  );
}
